# Hive MCP Gateway Configuration (Advanced)

# Note:
# - The GUI (Credential Manager + Settings) is the primary way to configure the app.
# - This file is optional and only used for advanced scenarios.
# - Host/port are typically set via GUI/config; env overrides apply only if exported.

# Anthropic API Configuration
# Get your API key from: https://console.anthropic.com/account/keys
ANTHROPIC_API_KEY=sk-ant-api03-...

# Optional: Default model for tool discovery
ANTHROPIC_MODEL=claude-3-opus-20240229

# Optional: Override default paths
MCP_SERVERS_PATH=mcp-servers.json
API_KEYS_PATH=.api-keys.json

# Server Configuration (optional overrides)
HOST=0.0.0.0
PORT=8001
